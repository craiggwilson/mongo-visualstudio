<UserControl x:Class="MongoDB.VisualStudio.Controls.Tree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MongoDB.VisualStudio.Models"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}">
    <TreeView ItemsSource="{Binding Children}">
        <TreeView.ItemContainerStyle>
            <Style TargetType="{x:Type TreeViewItem}">
                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                <Setter Property="FontWeight" Value="Normal" />
            </Style>
        </TreeView.ItemContainerStyle>
        <TreeView.Resources>
            <HierarchicalDataTemplate 
                DataType="{x:Type local:ServerViewModel}" 
                ItemsSource="{Binding Children}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="pack://application:,,,/MongoVS;component/Resources/Images/Server.png" Height="16" Width="16" />
                    <TextBlock Text="{Binding Name}" />
                </StackPanel>   
            </HierarchicalDataTemplate>

            <HierarchicalDataTemplate 
                DataType="{x:Type local:DatabasesViewModel}" 
                ItemsSource="{Binding Children}">
                <StackPanel Orientation="Horizontal">
                    <Image Name="image" Source="pack://application:,,,/MongoVS;component/Resources/Images/FolderClosed.png" Height="16" Width="16" />
                    <TextBlock Text="{Binding Name}">
                        <TextBlock.ContextMenu>
                            <ContextMenu ItemsSource="{Binding ContextMenuItems}">
                                <ContextMenu.ItemTemplate>
                                    <DataTemplate>
                                        <MenuItem Header="{Binding Name}" Command="{Binding Command}"/>
                                    </DataTemplate>
                                </ContextMenu.ItemTemplate>
                            </ContextMenu>
                        </TextBlock.ContextMenu>
                    </TextBlock>
                </StackPanel>
                <HierarchicalDataTemplate.Triggers>
                    <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                        <Setter TargetName="image" Property="Source" Value="pack://application:,,,/MongoVS;component/Resources/Images/FolderOpen.png"/>
                    </DataTrigger>
                </HierarchicalDataTemplate.Triggers>
            </HierarchicalDataTemplate>

            <HierarchicalDataTemplate 
                DataType="{x:Type local:DatabaseViewModel}" 
                ItemsSource="{Binding Children}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="pack://application:,,,/MongoVS;component/Resources/Images/Database.png" Height="16" Width="16" />
                    <TextBlock Text="{Binding Name}" />
                </StackPanel>
            </HierarchicalDataTemplate>

            <HierarchicalDataTemplate 
                DataType="{x:Type local:CollectionsViewModel}" 
                ItemsSource="{Binding Children}">
                <StackPanel Orientation="Horizontal">
                    <Image Name="image" Source="pack://application:,,,/MongoVS;component/Resources/Images/FolderClosed.png" Height="16" Width="16" />
                    <TextBlock Text="{Binding Name}" />
                </StackPanel>
                <HierarchicalDataTemplate.Triggers>
                    <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                        <Setter TargetName="image" Property="Source" Value="pack://application:,,,/MongoVS;component/Resources/Images/FolderOpen.png"/>
                    </DataTrigger>
                </HierarchicalDataTemplate.Triggers>
            </HierarchicalDataTemplate>

            <DataTemplate 
                DataType="{x:Type local:CollectionViewModel}">
                <StackPanel Orientation="Horizontal">
                    <Image Source="pack://application:,,,/MongoVS;component/Resources/Images/Collection.png" Height="16" Width="16" />
                    <TextBlock Text="{Binding Name}" />
                </StackPanel>
            </DataTemplate>
        </TreeView.Resources>
    </TreeView>
</UserControl>